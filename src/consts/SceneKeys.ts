enum SceneKeys {
    Preloader = 'preloader',
    Game = 'game',
    GameOver = 'game-over',
    StartGame = 'start-game',
    ProgressBar = 'progress-bar',
    TestScene = 'test-scene',
}

export default SceneKeys;